<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evan.screentest">
    `
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Top Pixel"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".deadPixelTestActivity" />
        <activity
            android:name=".redFull"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_red_full"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".greenFull"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_green_full"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".blueFull"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_blue_full"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".activity_contrast" />
        <activity
            android:name=".blackFull"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_black_full"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".activity_about"></activity>
    </application>

</manifest>